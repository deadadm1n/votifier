name: Build

on:
  push:
    branches: [main, 'mc/**']
  pull_request:
    branches: [main, 'mc/**']
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Detect Java version
        id: java
        run: |
          JAVA_VERSION=$(grep 'java_version=' gradle.properties | cut -d'=' -f2)
          echo "version=$JAVA_VERSION" >> "$GITHUB_OUTPUT"

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          java-version: ${{ steps.java.outputs.version }}
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build
        run: ./gradlew build

      - name: Verify JARs
        run: bash scripts/verify-jars.sh

      - name: Upload Forge artifact
        uses: actions/upload-artifact@v4
        with:
          name: forge-jar
          path: forge/build/libs/*.jar
        if: hashFiles('forge/build/libs/*.jar') != ''

      - name: Upload Fabric artifact
        uses: actions/upload-artifact@v4
        with:
          name: fabric-jar
          path: fabric/build/libs/*.jar

      - name: Upload NeoForge artifact
        uses: actions/upload-artifact@v4
        with:
          name: neoforge-jar
          path: neoforge/build/libs/*.jar
        if: hashFiles('neoforge/build/libs/*.jar') != ''

  server-smoke-test:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check for NeoForge subproject
        id: check
        run: |
          if [ -f neoforge/build.gradle ]; then
            echo "has_neoforge=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_neoforge=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Detect Java version
        if: steps.check.outputs.has_neoforge == 'true'
        id: java
        run: |
          JAVA_VERSION=$(grep 'java_version=' gradle.properties | cut -d'=' -f2)
          echo "version=$JAVA_VERSION" >> "$GITHUB_OUTPUT"

      - name: Set up JDK
        if: steps.check.outputs.has_neoforge == 'true'
        uses: actions/setup-java@v4
        with:
          java-version: ${{ steps.java.outputs.version }}
          distribution: 'temurin'

      - name: Setup Gradle
        if: steps.check.outputs.has_neoforge == 'true'
        uses: gradle/actions/setup-gradle@v4

      - name: Build project
        if: steps.check.outputs.has_neoforge == 'true'
        run: ./gradlew build

      - name: Accept EULA
        if: steps.check.outputs.has_neoforge == 'true'
        run: mkdir -p neoforge/run && echo "eula=true" > neoforge/run/eula.txt

      - name: Start server in background
        if: steps.check.outputs.has_neoforge == 'true'
        run: ./gradlew :neoforge:runServer > server.log 2>&1 &
        env:
          CI: true

      - name: Wait for Votifier to start
        if: steps.check.outputs.has_neoforge == 'true'
        run: |
          timeout=300
          elapsed=0
          while [ $elapsed -lt $timeout ]; do
            if [ -f server.log ]; then
              if grep -q "Votifier server started on" server.log; then
                echo "Votifier started successfully"
                exit 0
              fi
              if grep -qiE "crash|exception.*fatal|failed to load|ModLoadingException" server.log; then
                echo "Server crash detected!"
                tail -50 server.log
                exit 1
              fi
            fi
            sleep 5
            elapsed=$((elapsed + 5))
          done
          echo "Timed out after ${timeout}s waiting for Votifier to start"
          if [ -f server.log ]; then
            tail -100 server.log
          fi
          exit 1

      - name: Stop server
        if: always() && steps.check.outputs.has_neoforge == 'true'
        run: |
          pkill -f "runServer" || true
          pkill -f "minecraft" || true

      - name: Upload server log
        if: always() && steps.check.outputs.has_neoforge == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: server-log
          path: server.log
